diff --git a/poke-engine-py/src/lib.rs b/poke-engine-py/src/lib.rs
index 032da2e..f1006b7 100644
--- a/poke-engine-py/src/lib.rs
+++ b/poke-engine-py/src/lib.rs
@@ -41,6 +41,10 @@ pub struct PyState {
     pub weather_turns_remaining: i8,
     pub terrain: String,
     pub terrain_turns_remaining: i8,
+    pub base_weather: String,
+    pub base_weather_turns_remaining: i8,
+    pub base_terrain: String,
+    pub base_terrain_turns_remaining: i8,
     pub trick_room: bool,
     pub trick_room_turns_remaining: i8,
     pub team_preview: bool,
@@ -55,6 +59,10 @@ impl From<State> for PyState {
             weather_turns_remaining: other.weather.turns_remaining,
             terrain: other.terrain.terrain_type.to_string(),
             terrain_turns_remaining: other.terrain.turns_remaining,
+            base_weather: other.base_weather.weather_type.to_string(),
+            base_weather_turns_remaining: other.base_weather.turns_remaining,
+            base_terrain: other.base_terrain.terrain_type.to_string(),
+            base_terrain_turns_remaining: other.base_terrain.turns_remaining,
             trick_room: other.trick_room.active,
             trick_room_turns_remaining: other.trick_room.turns_remaining,
             team_preview: other.team_preview,
@@ -75,6 +83,14 @@ impl Into<State> for PyState {
                 terrain_type: Terrain::from_str(&self.terrain).unwrap(),
                 turns_remaining: self.terrain_turns_remaining,
             },
+            base_weather: StateWeather {
+                weather_type: Weather::from_str(&self.base_weather).unwrap(),
+                turns_remaining: self.base_weather_turns_remaining,
+            },
+            base_terrain: StateTerrain {
+                terrain_type: Terrain::from_str(&self.base_terrain).unwrap(),
+                turns_remaining: self.base_terrain_turns_remaining,
+            },
             trick_room: StateTrickRoom {
                 active: self.trick_room,
                 turns_remaining: self.trick_room_turns_remaining,
@@ -98,6 +114,10 @@ impl PyState {
         weather_turns_remaining=0,
         terrain="none".to_string(),
         terrain_turns_remaining=0,
+        base_weather="none".to_string(),
+        base_weather_turns_remaining=0,
+        base_terrain="none".to_string(),
+        base_terrain_turns_remaining=0,
         trick_room=false,
         trick_room_turns_remaining=0,
         team_preview=false,
@@ -109,6 +129,10 @@ impl PyState {
         weather_turns_remaining: i8,
         terrain: String,
         terrain_turns_remaining: i8,
+        base_weather: String,
+        base_weather_turns_remaining: i8,
+        base_terrain: String,
+        base_terrain_turns_remaining: i8,
         trick_room: bool,
         trick_room_turns_remaining: i8,
         team_preview: bool,
@@ -120,6 +144,10 @@ impl PyState {
             weather_turns_remaining,
             terrain,
             terrain_turns_remaining,
+            base_weather,
+            base_weather_turns_remaining,
+            base_terrain,
+            base_terrain_turns_remaining,
             trick_room,
             trick_room_turns_remaining,
             team_preview,
diff --git a/src/genx/generate_instructions.rs b/src/genx/generate_instructions.rs
index f359446..4df706d 100644
--- a/src/genx/generate_instructions.rs
+++ b/src/genx/generate_instructions.rs
@@ -2837,15 +2837,15 @@ fn add_end_of_turn_instructions(
         if state.weather.turns_remaining == 0 {
             on_weather_end(state, sides, &mut incoming_instructions);
             let weather_end_instruction = Instruction::ChangeWeather(ChangeWeather {
-                new_weather: Weather::NONE,
-                new_weather_turns_remaining: 0,
+                new_weather: state.base_weather.weather_type,
+                new_weather_turns_remaining: state.base_weather.turns_remaining,
                 previous_weather: state.weather.weather_type,
                 previous_weather_turns_remaining: 0,
             });
             incoming_instructions
                 .instruction_list
                 .push(weather_end_instruction);
-            state.weather.weather_type = Weather::NONE;
+            state.weather = state.base_weather.clone();
         }
     }

@@ -2900,15 +2900,15 @@ fn add_end_of_turn_instructions(
         if state.terrain.turns_remaining == 0 {
             on_terrain_end(state, sides, &mut incoming_instructions);
             let terrain_end_instruction = Instruction::ChangeTerrain(ChangeTerrain {
-                new_terrain: Terrain::NONE,
-                new_terrain_turns_remaining: 0,
+                new_terrain: state.base_terrain.terrain_type,
+                new_terrain_turns_remaining: state.base_terrain.turns_remaining,
                 previous_terrain: state.terrain.terrain_type,
                 previous_terrain_turns_remaining: 0,
             });
             incoming_instructions
                 .instruction_list
                 .push(terrain_end_instruction);
-            state.terrain.terrain_type = Terrain::NONE;
+            state.terrain = state.base_terrain.clone();
         }
     }

diff --git a/src/state.rs b/src/state.rs
index 2bf6db8..91af4e6 100644
--- a/src/state.rs
+++ b/src/state.rs
@@ -1323,6 +1323,8 @@ pub struct State {
     pub side_two: Side,
     pub weather: StateWeather,
     pub terrain: StateTerrain,
+    pub base_weather: StateWeather,
+    pub base_terrain: StateTerrain,
     pub trick_room: StateTrickRoom,
     pub team_preview: bool,
     pub use_last_used_move: bool,
@@ -1341,6 +1343,14 @@ impl Default for State {
                 terrain_type: Terrain::NONE,
                 turns_remaining: 0,
             },
+            base_weather: StateWeather {
+                weather_type: Weather::NONE,
+                turns_remaining: 0,
+            },
+            base_terrain: StateTerrain {
+                terrain_type: Terrain::NONE,
+                turns_remaining: 0,
+            },
             trick_room: StateTrickRoom {
                 active: false,
                 turns_remaining: 0,
@@ -2219,13 +2229,15 @@ impl State {

     pub fn serialize(&self) -> String {
         format!(
-            "{}/{}/{}/{}/{}/{}",
+            "{}/{}/{}/{}/{}/{}/{}/{}",
             self.side_one.serialize(),
             self.side_two.serialize(),
             self.weather.serialize(),
             self.terrain.serialize(),
             self.trick_room.serialize(),
-            self.team_preview
+            self.team_preview,
+            self.base_weather.serialize(),
+            self.base_terrain.serialize()
         )
     }

@@ -2423,6 +2435,16 @@ impl State {
             side_two: Side::deserialize(split[1]),
             weather: StateWeather::deserialize(split[2]),
             terrain: StateTerrain::deserialize(split[3]),
+            base_weather: if split.len() > 6 {
+                StateWeather::deserialize(split[6])
+            } else {
+                StateWeather { weather_type: Weather::NONE, turns_remaining: 0 }
+            },
+            base_terrain: if split.len() > 7 {
+                StateTerrain::deserialize(split[7])
+            } else {
+                StateTerrain { terrain_type: Terrain::NONE, turns_remaining: 0 }
+            },
             trick_room: StateTrickRoom::deserialize(split[4]),
             team_preview: split[5].parse::<bool>().unwrap(),
             use_damage_dealt: false,
diff --git a/tests/test_battle_mechanics.rs b/tests/test_battle_mechanics.rs
index d4cd426..f7b2ac0 100644
--- a/tests/test_battle_mechanics.rs
+++ b/tests/test_battle_mechanics.rs
@@ -27,7 +27,8 @@ use poke_engine::instruction::{
 use poke_engine::pokemon::PokemonName;
 use poke_engine::state::{
     pokemon_index_iter, LastUsedMove, Move, PokemonBoostableStat, PokemonIndex, PokemonMoveIndex,
-    PokemonSideCondition, PokemonStatus, PokemonType, SideReference, State, StateWeather,
+    PokemonSideCondition, PokemonStatus, PokemonType, SideReference, State, StateTerrain,
+    StateWeather,
 };

 #[cfg(feature = "terastallization")]
@@ -9202,6 +9203,58 @@ fn test_rain_ends_if_turns_remaining_is_1() {
     assert_eq!(expected_instructions, vec_of_instructions);
 }

+#[test]
+fn test_temporary_rain_restores_configured_permanent_sun() {
+    let mut state = State::default();
+    state.weather.weather_type = Weather::RAIN;
+    state.weather.turns_remaining = 1;
+    state.base_weather = StateWeather {
+        weather_type: Weather::SUN,
+        turns_remaining: -1,
+    };
+
+    let vec_of_instructions = set_moves_on_pkmn_and_call_generate_instructions(
+        &mut state,
+        Choices::SPLASH,
+        Choices::SPLASH,
+    );
+
+    assert!(vec_of_instructions[0].instruction_list.contains(
+        &Instruction::ChangeWeather(ChangeWeather {
+            new_weather: Weather::SUN,
+            new_weather_turns_remaining: -1,
+            previous_weather: Weather::RAIN,
+            previous_weather_turns_remaining: 0,
+        })
+    ));
+}
+
+#[test]
+fn test_temporary_electric_terrain_restores_configured_grassy_terrain() {
+    let mut state = State::default();
+    state.terrain.terrain_type = Terrain::ELECTRICTERRAIN;
+    state.terrain.turns_remaining = 1;
+    state.base_terrain = StateTerrain {
+        terrain_type: Terrain::GRASSYTERRAIN,
+        turns_remaining: 127,
+    };
+
+    let vec_of_instructions = set_moves_on_pkmn_and_call_generate_instructions(
+        &mut state,
+        Choices::SPLASH,
+        Choices::SPLASH,
+    );
+
+    assert!(vec_of_instructions[0].instruction_list.contains(
+        &Instruction::ChangeTerrain(ChangeTerrain {
+            new_terrain: Terrain::GRASSYTERRAIN,
+            new_terrain_turns_remaining: 127,
+            previous_terrain: Terrain::ELECTRICTERRAIN,
+            previous_terrain_turns_remaining: 0,
+        })
+    ));
+}
+
 #[test]
 fn test_sand_does_not_inflict_damage_when_ending() {
     let mut state = State::default();
