diff --git a/src/instruction.rs b/src/instruction.rs
index 835de45..68189a0 100644
--- a/src/instruction.rs
+++ b/src/instruction.rs
@@ -45,6 +45,19 @@ macro_rules! slotted_instruction {
     };
 }
 
+/// Renders a slotted instruction's active slot for Debug output: empty in a singles
+/// build (so singles output is unchanged byte for byte), `:<slot>` in a doubles build.
+/// Without this, `Damage SideTwo: 72` is ambiguous between the two actives, which makes
+/// doubles instruction lists unreadable and un-diffable.
+#[cfg(not(feature = "doubles"))]
+fn slot_tag(_slot: u8) -> String {
+    String::new()
+}
+#[cfg(feature = "doubles")]
+fn slot_tag(slot: u8) -> String {
+    format!(":{}", slot)
+}
+
 #[derive(PartialEq, Clone)]
 pub struct StateInstructions {
     pub percentage: f32,
@@ -151,15 +164,15 @@ impl fmt::Debug for Instruction {
             Instruction::ApplyVolatileStatus(a) => {
                 write!(
                     f,
-                    "ApplyVolatileStatus {:?}: {:?}",
-                    a.side_ref, a.volatile_status
+                    "ApplyVolatileStatus {:?}{}: {:?}",
+                    a.side_ref, slot_tag(a.slot()), a.volatile_status
                 )
             }
             Instruction::RemoveVolatileStatus(r) => {
                 write!(
                     f,
-                    "RemoveVolatileStatus {:?}: {:?}",
-                    r.side_ref, r.volatile_status
+                    "RemoveVolatileStatus {:?}{}: {:?}",
+                    r.side_ref, slot_tag(r.slot()), r.volatile_status
                 )
             }
             Instruction::ChangeStatus(c) => {
@@ -170,13 +183,13 @@ impl fmt::Debug for Instruction {
                 )
             }
             Instruction::Heal(h) => {
-                write!(f, "Heal {:?}: {:?}", h.side_ref, h.heal_amount)
+                write!(f, "Heal {:?}{}: {:?}", h.side_ref, slot_tag(h.slot()), h.heal_amount)
             }
             Instruction::Damage(d) => {
-                write!(f, "Damage {:?}: {}", d.side_ref, d.damage_amount)
+                write!(f, "Damage {:?}{}: {}", d.side_ref, slot_tag(d.slot()), d.damage_amount)
             }
             Instruction::Boost(b) => {
-                write!(f, "Boost {:?} {:?}: {:?}", b.side_ref, b.stat, b.amount)
+                write!(f, "Boost {:?}{} {:?}: {:?}", b.side_ref, slot_tag(b.slot()), b.stat, b.amount)
             }
             Instruction::ChangeSideCondition(c) => {
                 write!(
@@ -231,8 +244,11 @@ impl fmt::Debug for Instruction {
             Instruction::ChangeItem(c) => {
                 write!(
                     f,
-                    "ChangeItem {:?}: {:?} -> {:?}",
-                    c.side_ref, c.current_item, c.new_item
+                    "ChangeItem {:?}{}: {:?} -> {:?}",
+                    c.side_ref,
+                    slot_tag(c.slot()),
+                    c.current_item,
+                    c.new_item
                 )
             }
             Instruction::ChangeAttack(c) => {
@@ -275,8 +291,8 @@ impl fmt::Debug for Instruction {
             Instruction::DamageSubstitute(d) => {
                 write!(
                     f,
-                    "DamageSubstitute {:?}: {:?}",
-                    d.side_ref, d.damage_amount
+                    "DamageSubstitute {:?}{}: {:?}",
+                    d.side_ref, slot_tag(d.slot()), d.damage_amount
                 )
             }
             Instruction::DecrementRestTurns(d) => {
@@ -332,8 +348,8 @@ impl fmt::Debug for Instruction {
             Instruction::SetLastUsedMove(s) => {
                 write!(
                     f,
-                    "SetLastUsedMove {:?}: {:?} -> {:?}",
-                    s.side_ref, s.previous_last_used_move, s.last_used_move
+                    "SetLastUsedMove {:?}{}: {:?} -> {:?}",
+                    s.side_ref, slot_tag(s.slot()), s.previous_last_used_move, s.last_used_move
                 )
             }
             Instruction::ChangeDamageDealtDamage(s) => {
@@ -356,8 +372,8 @@ impl fmt::Debug for Instruction {
             Instruction::DecrementPP(s) => {
                 write!(
                     f,
-                    "DecrementPP {:?}: {:?} {}",
-                    s.side_ref, s.move_index, s.amount
+                    "DecrementPP {:?}{}: {:?} {}",
+                    s.side_ref, slot_tag(s.slot()), s.move_index, s.amount
                 )
             }
             Instruction::ToggleTrickRoom(i) => {
